# -*- mode: snippet -*-
# name: getopts block
# key: getopts()
# group: shell
# --
OPTIND=1
while getopts ${1::hx} ${2:opt}; do
$>    case \$$2 in
$>        ${3:h) usage ; exit 0 ;;}
$>        ${4:x) set -x ;;}
$>    esac
done
shift $(($OPTIND - 1))
$0