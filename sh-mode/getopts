# -*- mode: snippet -*-
# name: getopts block
# key: getopts_block
# group: shell
# --
OPTIND=1
while getopts ${1::v} ${2:opt}; do
$>    case \$$2 in
$>        ${3:v) ;;}
$>    esac
done
shift $(($OPTIND - 1))
$0